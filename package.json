{
  "name": "squash-weekly-poll",
  "version": "1.0.0",
  "description": "Example of creating a function that runs as a cron job using the serverless `schedule` event",
  "author": "",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "start": "nodemon dist/main.js",
    "dev": "npx concurrently \"npm:watch\" \"npm:start\""
  },
  "devDependencies": {
    "@types/node-fetch": "^2.6.2",
    "concurrently": "^7.3.0",
    "dotenv": "^16.0.1",
    "serverless-offline": "^9.2.4"
  },
  "dependencies": {
    "node-fetch": "2",
    "nodemon": "^2.0.19",
    "telegraf": "^4.8.6"
  }
}
